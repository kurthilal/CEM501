"""Demo agent package (reader, classifier, drafter, memory, sender, scheduler, Telegram bot).

Run from the ``project/`` directory, for example::

    python3 -m demo.scheduler --once --dry-run
    python3 -m demo.test_bot

Environment file expected at ``project/.env``.
"""
